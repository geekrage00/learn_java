public class Ternary{
    public static void main(String[] args) {
        // codition ? "option true" : "option false"
        boolean isRain = false;
        String message = isRain ? "take an umbrella" : "go walk without umbrella";

        System.out.println(message);
    }
}